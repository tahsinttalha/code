#include <iostream> // works like <stdio.h> in c

using namespace std; // saves the trouble to write std::cout or std::cin every time

int main()
{
    int a = 0, b = 1;
    
    cout << a << endl // prints output in the console
         << b;

    return 0;
}