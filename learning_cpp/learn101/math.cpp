#include <iostream>
#include <cmath> // includes math functions

using namespace std;

int main()
{
    double result =  floor(1.2);
    double power = pow(2, 3);
    cout << result << endl
         << power << endl;
}