#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

int main() {
    int t;
    cin >> t;
    
    while (t--)
    {
        int n, last_value, last_index;
        cin >> n;

        vector <int> array (n);
        for (int i = 0; i < n; i++)
        {
            cin >> array[i];
        }

        
    }
}